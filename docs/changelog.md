# Changelog — Smart Knowledge Factory

## 2026-05-29

### Добавлено
- Мультиязычность (РУС/ҚАЗ): i18next, переключатель в сайдбаре и на странице обучения
- Удаление сотрудников: кнопка в строке + массовое удаление с чекбоксами
- Перемешивание вариантов ответа в квизе (Fisher-Yates shuffle на каждый маунт QuizBlock)
- `CLAUDE.md` — контекст проекта для Claude
- Slash-команды: `/deploy`, `/update-docs`, `/check`
- `docs/changelog.md` — история изменений

### Исправлено
- Логика квиза: 1-я ошибка → попап "попробуйте ещё раз" (без перезапуска видео), 2-я ошибка → перезапуск видео
- Перемотка вперёд при повторе видео: VideoPlayer ремаунтится через `key={lessonId-replayCount}` с `maxAllowedPosition=0`
- Баг после обновления страницы: `videoMaxPosition` стал вычисляемым (не state) — устранена задержка React рендера
- Видео скрывается полностью при показе вопроса (вместо миниатюры)

### Изменено
- Режим квиза переименован: "Квиз" → "Режим тестирования", "С кнопкой" → "С подтверждением", "Без кнопки" → "Без подтверждения"
- Переводы super admin страниц (`super/index.tsx`, `super/landing.tsx`) — все TODO: i18n закрыты
- Удалены неиспользуемые файлы: `mock-data.ts`, `constants.ts`, `deploy.bat`, `app.config.timestamp_*.js`
